# 更新日志

所有值得关注的更改都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.4.2] - 2026-08-06

### 🐛 问题修复

- **模型编辑页在请求失败时渲染空白表单** — `/admin/models/<id>` 的加载逻辑没有检查 `res.ok`，因此错误响应会被当作模型数据解析。此时所有字段都是 `undefined`，后端列表进而落入「尚无后端」的兜底分支，渲染出一行空白后端。页面看上去就像该模型的后端 URL、真实模型名与 API Key 全部丢失，实际上只是请求失败了——升级到 0.4.x 后若代码已上线而 `npm run migrate` 尚未执行，缺失的 `model_backends` 表会让接口返回 500，该现象尤为明显。现在加载逻辑会拒绝非 2xx 响应，并直接展示失败原因（含服务端返回的错误信息），而不再显示一个可编辑的空白表单。用户与分组详情页原本已有该判断，未作改动。

## [0.4.1] - 2026-08-06

### 🐛 问题修复

- **无法删除用户** — `usage_logs.user_id` 与 `daily_usage.user_id` 对 `users(id)` 的外键为 `ON DELETE NO ACTION`，因此只要用户发起过任何一次请求，删除时就会触发外键约束冲突。由于接口未捕获该异常，最终表现为不带错误信息的 500，后台仅弹出一个内容为空的错误提示，看上去就像点击删除毫无反应；而没有任何用量记录的用户可以正常删除，使问题看起来时有时无。现将两处外键改为 `ON DELETE SET NULL`：用户账号被删除，其用量记录以匿名形式保留，仪表盘的历史统计因此保持准确。同时接口会将残余的外键冲突转换为可读的 `409`，前端在错误响应不是 JSON 时也会回退到通用提示文案。
- **`users.group_id` 表结构漂移** — 分组功能的首个迁移在自动生成的语句之后手工追加了 `SET NOT NULL`，Drizzle 并未记录该约束：此后所有快照都将该列声明为可空，也就永远不会生成修正迁移。而应用各层始终按「允许没有分组」处理——代理层回退到按用户配额，管理接口有显式的 `groupId: null` 分支，用户列表显示 `—`。因此向更新接口传入 `groupId: null`，或在不存在默认分组时创建用户，都会失败并返回难以定位的 500。现已将数据库与代码中声明的表结构对齐，使这些路径按设计正常工作。
- **模型别名校验静默失效** — 别名输入框的 HTML `pattern` 属性在字符类中直接使用了未转义的 `-` 与 `/`。浏览器会以正则 `v` 标志编译该属性，而 `v` 模式不接受这两个字符；按照 HTML 规范，无法编译的 `pattern` 会被完全忽略——因此该字段在前端接受任意输入，并且每次渲染模型表单都会向控制台输出 `SyntaxError`。此外该规则比服务端更严格，会误拒 `openai/gpt-4o` 这类合法别名。现在表单直接复用共享的 `MODEL_ALIAS_PATTERN`，前后端校验规则不会再出现分歧，别名提示文案也更正为实际允许的分隔符。

## [0.4.0] - 2026-08-05

### ✨ 多后端负载均衡

- **一个模型别名 → 多台上游后端** — 模型现在由「公共别名 + 后端列表」（新增 `model_backends` 表）组成，同一模型部署在多台 vLLM 服务器上时不再需要 `-1`/`-2` 后缀别名。存量模型自动迁移（各自成为单后端模型）；已有的后缀别名可在后台将其后端合并到同一个模型下、删除多余别名。配额、授权与用量统计仍以模型为键，天然聚合所有后端，无需任何调整。
- **缓存亲和路由 + 自动故障切换** — chat/completions 请求按对话的稳定首部（system prompt + 首条 user 消息）做 rendezvous（HRW）哈希路由：同一对话的每一轮固定落在同一台后端，命中 vLLM 前缀缓存；不同对话在统计上均匀分布。Embeddings/rerank 请求轮询分发。后端连接失败或返回 5xx/429 时自动切换下一台（流式请求在开始输出前同样可切换）；非 429 的 4xx 直接返回。模型无任何启用后端时返回 `503 backend_unavailable`。Anthropic 兼容端点使用同一套路由。亲和哈希长度上限可通过 `AFFINITY_PREFIX_LENGTH` 调整（默认 16384）。
- **后台多后端管理** — 模型表单支持管理后端列表（每台后端独立的 URL / 真实模型名 / API Key / 启用开关），模型列表显示后端数量摘要，**测试**按钮现在会逐台探测模型的所有后端（或通过 `{ backendId }` 只测某一台）并返回逐台结果。管理 API 的创建/更新接口接受嵌套的 `backends` 数组，同时兼容旧的扁平 `backendUrl`/`backendModel`/`backendApiKey` 字段（视为单后端简写）。审计日志脱敏改为递归处理嵌套结构，确保各后端的 API Key 不会泄露。
- **跨后端上下文窗口** — `GET /api/v1/models`（及单模型接口）的 `max_model_len` 现在取模型所有启用后端公布窗口的最小值，即无论请求落在哪台后端都安全的值。

### ✨ 重排序（Rerank）模型支持

- **兼容 OpenAI 的 `/api/v1/rerank` 接口** — 网关现在通过与 chat、embeddings 完全相同的鉴权、授权、配额、限流与用量记账管线代理 rerank 请求（`{ query, documents }`）。请求被转发到 `{backendUrl}/rerank`，因此只需配置模型的后端 URL，即可接入任意兼容 Jina / vLLM / Xinference / TEI / Cohere 的重排序后端。Rerank 始终为非流式；当上游返回时记录 `prompt_tokens`/`total_tokens`（`completion_tokens = 0`）。无需数据库迁移。
- **`rerank` 模型类型** — 在后台注册或编辑模型时，模型类型新增 `rerank`（与 `chat`、`embedding` 并列），后台的**测试**按钮会以最小载荷探测真实的 `/rerank` 接口。网关强制校验：`/api/v1/rerank` 只能用于 `rerank` 模型，否则返回 `404 model_type_mismatch`。该类型同时在 `GET /api/v1/models` 中返回。

## [0.3.0] - 2026-07-13

### ✨ 向量（Embedding）模型支持

- **兼容 OpenAI 的 `/api/v1/embeddings` 接口** — 网关现在通过与 chat/completions 完全相同的鉴权、授权、配额、限流与用量记账管线代理 embedding 请求。Embedding 仅记录 prompt tokens（`completion_tokens = 0`），用量日志无需数据库迁移。
- **模型 `type` 字段** — 每个模型新增类型 `chat`（默认）或 `embedding`，在后台注册或编辑模型时设置。网关强制校验：`/api/v1/embeddings` 只能用于 `embedding` 模型，chat/completions 接口只能用于 `chat` 模型，否则返回 `404 model_type_mismatch`。该类型同时在 `GET /api/v1/models` 中返回。

## [0.2.0] - 2026-07-01

### ✨ 管理后台审计日志

- **追加型审计记录** — 现在会将所有管理员的变更操作（用户、分组、模型、配额及模型授权的增删改）以及安全事件（管理员登录、登录失败、登出、「以用户身份查看」越权切换）记录到独立的 `audit_logs` 表。记录采用异步写入，审计不会拖慢或影响原操作本身。
- **完整且安全的记录** — 每条记录包含操作管理员、操作类型、目标对象（附带快照标签，即使目标日后被删除仍然可读）、字段级别的变更前后对比、客户端 IP、User Agent 以及成功/失败状态。敏感字段（密码哈希、API Key、后端密钥）一律脱敏，并在写入层统一强制执行，确保密钥永远不会进入审计日志。
- **审计仪表页** — 后台侧边栏新增只读的「审计日志」页面，支持按管理员、操作类型、对象类型、状态、日期范围及关键词筛选，可查看任意事件的变更前后详情，并将筛选结果导出为 CSV。

## [0.1.3] - 2026-04-27

### 🐛 问题修复

- **以用户身份查看** — 修复管理员从后台切换到用户视角时的跳转链路。在反向代理场景下，现在会先通过临时 URL token 完成一次中转，再由中间件写入用户会话 Cookie 并重定向到干净的 `/dashboard` 地址，避免链接指向错误地址，以及重定向响应里写 Cookie 不稳定的问题。
- **管理后台 · 用户列表返回定位** — 从 `/admin/users/[id]` 返回列表时，现在会保留原来的分页和搜索条件。编辑页会携带来源列表的 `back` 查询参数，返回按钮可精确回到进入详情前的筛选上下文，不再总是回到第一页。

## [0.1.2] - 2026-04-25

### 🐛 问题修复

- **管理后台 · 用户组详情页** — 用户选择下拉框因父容器设有 `overflow-hidden` 被裁剪，导致其高度极小、搜索框无法使用。从 `SectionCard` 外层去掉 `overflow-hidden` 从根源解决该问题。
- **SearchableSelect 组件** — 选项文字现在超出宽度时以省略号截断，不再换行；下拉框最小宽度调整为 280px，足以完整显示「姓名 (邮箱)」格式的内容。新增可选 `searchText` 字段，允许调用方传入独立的检索文本（与显示标签解耦），确保按姓名或邮箱的任意片段均可命中。

---

## [0.1.1] - 2026-04-25

### 🔧 工程改善

- 新增版本号自动管理与双语 Changelog 自动生成工具（`npm run release` / `npm run changelog`）
- 引入 commitlint + husky，提交时自动校验 Conventional Commits 规范

---

## [0.1.0] - 2026-04-25

首个正式版本。包含完整的代理网关核心功能、管理后台、用户分组权限体系、用户自助门户及国际化支持。

### ✨ 用户分组与权限管理

完整的多租户分组体系，支持基于组的模型访问控制和配额管理：

- 新增用户分组系统（Groups），组成员自动继承组级别的模型访问权限和使用配额
- 管理员可在分组详情页增删成员；删除分组时，成员自动回归至 Default 组
- Default 组作为兜底分组，其配额设置不影响成员（个人配额优先）
- 管理后台提供完整的分组 CRUD API

### ✨ 用户自助门户

- 新增用户登录页面（`/login`）和注册页面（含管理员审批流程）
- 新增用户仪表盘（`/dashboard`）：展示使用量趋势、剩余配额、API 密钥（含一键复制）及 Python/curl 示例代码
- 仪表盘提供近 7 天各模型 Token 消耗饼图

### ✨ 中英文国际化

- 全站支持中英文切换，覆盖管理后台和用户仪表盘所有页面
- 侧边栏和顶部导航均提供语言切换按钮
- 语言优先级：localStorage 已保存偏好 → 浏览器语言 → 英文回退

### ✨ 管理后台增强

- 使用量仪表盘新增时间范围切换（7 / 14 / 30 天）
- 用户列表支持分页，搜索和分页状态通过 URL 参数持久化
- 新增「以用户身份查看」功能：管理员无需用户密码即可查看其仪表盘
- 使用日志支持从用户列表一键跳转并自动过滤
- 新增可搜索下拉选择器（SearchableSelect）组件，用于用户/模型选择场景
- 请求日志中完整展示提示词内容（不再截断）

### 🐛 问题修复

- 修复语言检测在 SSR 与 CSR 之间的 hydration 不匹配（React 水合错误）
- 修复非管理员用户在管理后台无法更新密码的问题
- 修复上游代理错误消息过于技术化、不友好的问题
- 修复跨环境（HTTPS / 无 `navigator.clipboard`）剪贴板复制失败
- 修复用户列表 SQL 数组查询在空条件下崩溃
- 修复管理员首次登录时未触发 seed 的问题
- 修复 Recharts 图表 tooltip 格式化函数类型错误

### 📝 文档

- 新增中文系统架构设计文档
- README（中英文）详细补充项目初始化、数据库配置及生产部署说明
- 说明 `npm run migrate` 在生产环境的迁移工作流

### 🔧 基础设施

- 新增数据库查询性能索引（`idx_daily_usage_date`、`idx_usage_logs_created_at`）
- 使用量概览 API 增加 5 分钟内存 TTL 缓存，减少重复 DB 查询
- 新增 `npm run migrate` 命令，封装 drizzle-kit migrator 用于生产部署
